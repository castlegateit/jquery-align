(function($){"use strict";var resizeDone;var uniformify=function(collection){var done=[];collection.each(function(i,element){var box=$(element);var max=0;var boxes=box.add(box.siblings().filter(function(){return box.offset().top===$(this).offset().top}));if($.inArray(element,done)!==-1){return}boxes.each(function(i,domBox){var box=$(domBox);var height;box.height("auto");height=box.height();if(height>max){max=height}done.push(domBox)});if(boxes.length===1){return}boxes.each(function(i,domBox){$(domBox).height(max)})})};$.fn.uniformify=function(options){var object=this;var settings=$.extend({timeout:100});if(object.length<2){return object}$(window).on("resize",function(){clearTimeout(resizeDone);resizeDone=setTimeout(function(){$(window).trigger("resizeDone")},settings.timeout)});$(window).on("load resizeDone",function(){uniformify(object)});return object}})(jQuery);
